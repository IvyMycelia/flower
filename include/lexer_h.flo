    
    // === Primitives ===

TOKEN_INT:          int = 1     // Integer
TOKEN_FLOAT:        int = 2     // Float-point value
TOKEN_DOUBLE:       int = 3     // Double-point value
TOKEN_BOOL:         int = 4     // true / false, 1 / 0
TOKEN_CHAR:         int = 5     // Characters (represented as integers)

    // === Types ===

TOKEN_STRING:       int = 6     // Array of char (char*)
TOKEN_STRUCT:       int = 7     // Struct keyword
TOKEN_UNION:        int = 8
TOKEN_NULL:         int = 9     // Empty value

    // === Unary Operations ===

TOKEN_TILDE:        int = 10     // bitwise operation
    
    // === Identifiers === 

TOKEN_IDENTIFIER:   int = 11    // Variable & Function names
TOKEN_NUMBER:       int = 12    // Number
TOKEN_FLOAT_LIT:    int = 13    // Decimal number
TOKEN_STRING_LIT:   int = 14    // String literal "Hello, World!"
TOKEN_CHAR_LIT:     int = 15    // Char literal 'a'
TOKEN_VOID:         int = 16    // Void (empty)
    
    // === Keywords ===

TOKEN_RETURN:       int = 17    // Return from block
TOKEN_CONTINUE:     int = 18    // Skip a loop iteration
TOKEN_BREAK:        int = 19    // Stop a loop
TOKEN_WHILE:        int = 20    // While loop
TOKEN_FUNC:         int = 21    // Functions
TOKEN_FOR:          int = 22    // For loop
TOKEN_IN:           int = 23    // in
TOKEN_END:          int = 24    // Close block
TOKEN_IF:           int = 25    // If statement
TOKEN_ELSE:         int = 26    // Else block
TOKEN_NOT:          int = 27    // opposite of a value
TOKEN_AND:          int = 28    // logical and
TOKEN_OR:           int = 29    // logical or

    // === Import System ===

TOKEN_IMPORT:       int = 30    // .flo imports
TOKEN_FORWARD:      int = 31    // forward declarations
TOKEN_AS:           int = 32    // Alias for imports
TOKEN_PROP:         int = 33    // Propagating functions (making them public)
TOKEN_HIDDEN:       int = 34    // Field hidden outside module
TOKEN_READONLY:     int = 35    // Field readable but not externally writable
TOKEN_FROZEN:       int = 36    // Field assignable once

    // === Memory Management ===

TOKEN_NEW:          int = 37    // Automatically allocate memory
TOKEN_PRUNE:        int = 38    // Free memory
TOKEN_SIZEOF:       int = 39    // sizeof(x)
    
    // === stdio ===

TOKEN_PRINT:        int = 40    // Prints out to console

    // === Operators ===

TOKEN_DOT:          int = 41    // Dot access
TOKEN_DOTDOT:       int = 42    // ..
TOKEN_DOTDOTEQ:     int = 43    // ..=
TOKEN_PLUS:         int = 44    // Addition
TOKEN_MINUS:        int = 45    // Subtraction
TOKEN_STAR:         int = 46    // Multiplication
TOKEN_CARET:        int = 47    // ^ for exponents (eventually)
TOKEN_SLASH:        int = 48    // Division
TOKEN_AMPERSAND:    int = 49    // Memory address
TOKEN_AT:           int = 50    // Pointer / deref, @
TOKEN_ASSIGN:       int = 51    // Assign value
TOKEN_LT:           int = 52    // Less than
TOKEN_GT:           int = 53    // Greater than
TOKEN_COMP:         int = 54    // Comparison `==`
TOKEN_NEQ:          int = 55    // !=
TOKEN_GEQ:          int = 56    // >=
TOKEN_LEQ:          int = 57    // <=

    // === Punctuation ===

TOKEN_LPAREN:       int = 58    // Function handling
TOKEN_RPAREN:       int = 59    // Function handling
TOKEN_LBRACK:       int = 60    // Array handling
TOKEN_RBRACK:       int = 61    // Array handling
TOKEN_LBRACE:       int = 62    // Struct handling
TOKEN_RBRACE:       int = 63    // Struct handling
TOKEN_COLON:        int = 64    // Start block
TOKEN_COMMA:        int = 65    // Separate parameters
TOKEN_SEMI:         int = 66    // Semicolon for starting a new statement

TOKEN_NEWLINE:      int = 67    // For determining next statement

TOKEN_EOF:          int = 68    // End of File

struct Token {
    kind: int,
    start: int,  // Index into source buffer
    length: int  // Length of token
}

struct TokenStream {
    data: @Token,
    count: int,
    capacity: int
}

prop func get_line(src: @char, pos: int): int
    line: int = 1;
    for i in 0..pos:
        if src[i] == '\n':
            line = line + 1;
        end
    end
    return line;
end

prop func get_col(src: @char, pos: int): int
    col: int = 1
    while pos > 0 and src[pos - 1] != '\n':
        col = col + 1
        pos = pos - 1
    end
    return col
end
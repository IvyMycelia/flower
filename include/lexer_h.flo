    
    // === Primitives ===

TOKEN_INT:          int = 1     // Integer
TOKEN_FLOAT:        int = 2     // Float-point value
TOKEN_DOUBLE:       int = 3     // Double-point value
TOKEN_BOOL:         int = 4     // true / false, 1 / 0
TOKEN_CHAR:         int = 5     // Characters (represented as integers)

    // === Types ===

TOKEN_STRING:       int = 6     // Array of char (char*)
TOKEN_STRUCT:       int = 7     // Struct keyword
TOKEN_UNION:        int = 8
TOKEN_NULL:         int = 9     // Empty value

    // === Unary Operations ===

TOKEN_TILDE:        int = 10     // bitwise operation
    
    // === Identifiers === 

TOKEN_IDENTIFIER:   int = 11    // Variable & Function names
TOKEN_NUMBER:       int = 12    // Number
TOKEN_FLOAT_LIT:    int = 13    // Decimal number
TOKEN_STRING_LIT:   int = 14    // String literal "Hello, World!"
TOKEN_CHAR_LIT:     int = 15    // Char literal 'a'
TOKEN_VOID:         int = 16    // Void (empty)
    
    // === Keywords ===

TOKEN_RETURN:       int = 17    // Return from block
TOKEN_CONTINUE:     int = 18    // Skip a loop iteration
TOKEN_BREAK:        int = 19    // Stop a loop
TOKEN_WHILE:        int = 20    // While loop
TOKEN_FUNC:         int = 21    // Functions
TOKEN_FOR:          int = 22    // For loop
TOKEN_IN:           int = 23    // in
TOKEN_END:          int = 24    // Close block
TOKEN_IF:           int = 25    // If statement
TOKEN_ELSE:         int = 26    // Else block
TOKEN_NOT:          int = 27    // opposite of a value
TOKEN_AND:          int = 28    // logical and
TOKEN_OR:           int = 29    // logical or

    // === Import System ===

TOKEN_IMPORT:       int = 30    // .flo imports
TOKEN_FORWARD:      int = 31    // forward declarations
TOKEN_AS:           int = 32    // Alias for imports
TOKEN_PROP:         int = 33    // Propagating functions (making them public)

    // === Memory Management ===

TOKEN_NEW:          int = 34    // Automatically allocate memory
TOKEN_PRUNE:        int = 35    // Free memory
TOKEN_SIZEOF:       int = 36    // sizeof(x)
    
    // === stdio ===

TOKEN_PRINT:        int = 37    // Prints out to console

    // === Operators ===

TOKEN_DOT:          int = 38    // Dot access
TOKEN_DOTDOT:       int = 39    // ..
TOKEN_DOTDOTEQ:     int = 40    // ..=
TOKEN_PLUS:         int = 41    // Addition
TOKEN_MINUS:        int = 42    // Subtraction
TOKEN_STAR:         int = 43    // Multiplication
TOKEN_CARET:        int = 44    // ^ for exponents (eventually)
TOKEN_SLASH:        int = 45    // Division
TOKEN_AMPERSAND:    int = 46    // Memory address
TOKEN_AT:           int = 47    // Pointer / deref, @
TOKEN_ASSIGN:       int = 48    // Assign value
TOKEN_LT:           int = 49    // Less than
TOKEN_GT:           int = 50    // Greater than
TOKEN_COMP:         int = 51    // Comparison `==`
TOKEN_NEQ:          int = 52    // !=
TOKEN_GEQ:          int = 53    // >=
TOKEN_LEQ:          int = 54    // <=

    // === Punctuation ===

TOKEN_LPAREN:       int = 55    // Function handling
TOKEN_RPAREN:       int = 56    // Function handling
TOKEN_LBRACK:       int = 57    // Array handling
TOKEN_RBRACK:       int = 58    // Array handling
TOKEN_LBRACE:       int = 59    // Struct handling
TOKEN_RBRACE:       int = 60    // Struct handling
TOKEN_COLON:        int = 61    // Start block
TOKEN_COMMA:        int = 62    // Separate parameters
TOKEN_SEMI:         int = 63    // Semicolon for starting a new statement

TOKEN_NEWLINE:      int = 64    // For determining next statement

TOKEN_EOF:          int = 65    // End of File

struct Token {
    kind: int,
    start: int,  // Index into source buffer
    length: int  // Length of token
}

struct TokenStream {
    data: @Token,
    count: int,
    capacity: int
}

prop int get_line(src: @char, pos: int):
    line: int = 1;
    for i in 0..pos:
        if src[i] == '\n':
            line = line + 1;
        end
    end
    return line;
end

prop int get_col(src: @char, pos: int):
    col: int = 1
    while pos > 0 and src[pos - 1] != '\n':
        col = col + 1
        pos = pos - 1
    end
    return col
end
    
    // === Primitives ===

TOKEN_INT:          int = 1     // Integer
TOKEN_FLOAT:        int = 2     // Float-point value
TOKEN_DOUBLE:       int = 3     // Double-point value
TOKEN_BOOL:         int = 4     // true / false, 1 / 0
TOKEN_CHAR:         int = 5     // Characters (represented as integers)

    // === Types ===

TOKEN_STRING:       int = 6     // Array of char (char*)
TOKEN_STRUCT:       int = 7     // Struct keyword
TOKEN_UNION:        int = 8
TOKEN_NULL:         int = 9     // Empty value
TOKEN_TYPE:         int = 10    // type alias declaration

    // === Unary Operations ===

TOKEN_TILDE:        int = 11     // bitwise operation
    
    // === Identifiers === 

TOKEN_IDENTIFIER:   int = 12    // Variable & Function names
TOKEN_NUMBER:       int = 13    // Number
TOKEN_FLOAT_LIT:    int = 14    // Decimal number
TOKEN_STRING_LIT:   int = 15    // String literal "Hello, World!"
TOKEN_CHAR_LIT:     int = 16    // Char literal 'a'
TOKEN_VOID:         int = 17    // Void (empty)
TOKEN_TRUE:         int = 18
TOKEN_FALSE:        int = 19
    
    // === Keywords ===

TOKEN_RETURN:       int = 20    // Return from block
TOKEN_CONTINUE:     int = 21    // Skip a loop iteration
TOKEN_BREAK:        int = 22    // Stop a loop
TOKEN_WHILE:        int = 23    // While loop
TOKEN_FUNC:         int = 24    // Functions
TOKEN_FOR:          int = 25    // For loop
TOKEN_IN:           int = 26    // in
TOKEN_END:          int = 27    // Close block
TOKEN_IF:           int = 28    // If statement
TOKEN_ELSE:         int = 29    // Else block
TOKEN_NOT:          int = 30    // opposite of a value
TOKEN_AND:          int = 31    // logical and
TOKEN_OR:           int = 32    // logical or
TOKEN_IS:           int = 33    // type is

    // === Import System ===

TOKEN_IMPORT:       int = 34    // .flo imports
TOKEN_FORWARD:      int = 35    // forward declarations
TOKEN_AS:           int = 36    // Alias for imports
TOKEN_PROP:         int = 37    // Propagating functions (making them public)
TOKEN_HIDDEN:       int = 38    // Field hidden outside module
TOKEN_READONLY:     int = 39    // Field readable but not externally writable
TOKEN_FROZEN:       int = 40    // Field assignable once

    // === Memory Management ===

TOKEN_NEW:          int = 41    // Automatically allocate memory
TOKEN_PRUNE:        int = 42    // Free memory
TOKEN_SIZEOF:       int = 43    // sizeof(x)
    
    // === stdio ===

TOKEN_PRINT:        int = 44    // Prints out to console

    // === Operators ===

TOKEN_DOT:          int = 45    // Dot access
TOKEN_DOTDOT:       int = 46    // ..
TOKEN_DOTDOTEQ:     int = 47    // ..=
TOKEN_PLUS:         int = 48    // Addition
TOKEN_MINUS:        int = 49    // Subtraction
TOKEN_STAR:         int = 50    // Multiplication
TOKEN_CARET:        int = 51    // ^ for exponents (eventually)
TOKEN_SLASH:        int = 52    // Division
TOKEN_AMPERSAND:    int = 53    // Memory address
TOKEN_AT:           int = 54    // Pointer / deref, @
TOKEN_ASSIGN:       int = 55    // Assign value
TOKEN_LT:           int = 56    // Less than
TOKEN_GT:           int = 57    // Greater than
TOKEN_COMP:         int = 58    // Comparison `==`
TOKEN_NEQ:          int = 59    // !=
TOKEN_GEQ:          int = 60    // >=
TOKEN_LEQ:          int = 61    // <=

    // === Punctuation ===

TOKEN_LPAREN:       int = 62    // Function handling
TOKEN_RPAREN:       int = 63    // Function handling
TOKEN_LBRACK:       int = 64    // Array handling
TOKEN_RBRACK:       int = 65    // Array handling
TOKEN_LBRACE:       int = 66    // Struct handling
TOKEN_RBRACE:       int = 67    // Struct handling
TOKEN_COLON:        int = 68    // Start block
TOKEN_COMMA:        int = 69    // Separate parameters
TOKEN_QUESTION:     int = 70    // Nullable types
TOKEN_PIPE:         int = 71    // Semantic Union types
TOKEN_SEMI:         int = 72    // Semicolon for starting a new statement

TOKEN_NEWLINE:      int = 73    // For determining next statement

TOKEN_EOF:          int = 74    // End of File

struct Token {
    kind: int,
    start: int,  // Index into source buffer
    length: int  // Length of token
}

struct TokenStream {
    data: @Token,
    count: int,
    capacity: int
}

prop func get_line(src: @char, pos: int): int
    line: int = 1;
    for i in 0..pos:
        if src[i] == '\n':
            line = line + 1;
        end
    end
    return line;
end

prop func get_col(src: @char, pos: int): int
    col: int = 1
    while pos > 0 and src[pos - 1] != '\n':
        col = col + 1
        pos = pos - 1
    end
    return col
end
func fib(n: int): int
    a: int = 0
    b: int = 1
    i: int = 0

    while i < n:
        temp: int = b

        b = a + b
        a = temp
        i = i + 1 // no i++ or i += yet because that can be bootstrapped.
    end

    return a
end

prop func test(): int
    return fib(10)    
end
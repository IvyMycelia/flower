import <stdio>

prop func test(): bool
    name: string = "Ivy" as string
    raw: @char = name as @char
    other: string = raw as string

    if name != other:
        return false
    end

    if name.length != 3:
        return false
    end
    
    print(name)
    print("\n")

    return true
end